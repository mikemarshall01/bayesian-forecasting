# environments
.venv/
__pycache__/
*.pyc

# secrets
.env

# the small Parquet fixtures in data/ are committed on purpose so the notebook runs offline



# jupyter
.ipynb_checkpoints/

# PyMC / ArviZ sampling artefacts
*.nc

# committed on purpose: assets/*.png (so charts render in the README without running),
# and the executed notebooks/*.ipynb (so GitHub shows the output inline).
